要先启动.py再启动java代码

定位：基于大数据思想的AI项目，同时具有java工程化和分布式部署等优势

flink批流一体：
用Logstash采集网站日志，伪实时，如果定时拉取可以认为是批处理
用户聊天作为实时强信号作为实时流
Redis作为存储缓冲，用户事件导致画像变化，这里采用3次一根新，将写入效率增加三倍

分布式部署：
部署Redis主从+Sentinel，提供高可用性
部署一主一从多数据源数据库，缓解读压力
部署Nacos集群和Sentinel，提供管理、治理等多方面能力
